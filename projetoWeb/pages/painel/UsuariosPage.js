/**
 * Page Object — Painel / Usuarios
 *
 * STATUS: Skeleton — DOM não mapeado ainda.
 * Preencher seletores após DOM inspection no staging.
 */
export class UsuariosPage {
  constructor(page) {
    this.page = page;
  }

  // TODO: adicionar seletores após DOM inspection

  async goto() {
    // TODO: definir rota correta
    throw new Error('UsuariosPage.goto() não implementado — mapear DOM primeiro');
  }
}
